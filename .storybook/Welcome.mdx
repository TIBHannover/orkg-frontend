# Introduction

Here we document some of the components used in the [ORKG Frontend](https://orkg.org). The scope of this component library includes:

-   components that are reusable (i.e., they are made to be used more than once in the codebase)
-   components that are usable in isolation (i.e., not relying on a global store)

The API endpoints from [sandbox.orkg.org](sandbox.orkg.org) are used in the stories to ensure the components are functional and display actual data. API endpoints are not mocked.

## Bootstrap components

The ORKG frontend relies mainly on [Bootstrap](https://getbootstrap.com/) using [Reactstrap](https://reactstrap.github.io/). We do not duplicate the documentation from Reactstrap, so please refer to their documentation to find the majority of components used in the ORKG. We always recommend to use their components instead of creating new ones yourself. Only if components are missing, or are not well suited, we create our own components.

## Color palette

<table className="table">
  <thead>
    <tr>
      <th align="center">Name</th>
      <th align="center">Variable name</th>
      <th align="center">Color</th>
      <th>Preview</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td align="center">Primary color</td>
      <td align="center"><code>$primary</code></td>
      <td align="center">#e86161</td>
      <td><div style={{background: '#e86161', width: '90px', height: '25px', display: 'inline-block'}}/></td>
    </tr>
    <tr>
      <td align="center">Primary color - darker</td>
      <td align="center"><code>$primary-darker</code></td>
      <td align="center">#c61d1d</td>
      <td><div style={{background: '#c61d1d', width: '90px', height: '25px', display: 'inline-block'}}/></td>
    </tr>
    <tr>
      <td align="center">Secondary color</td>
      <td align="center"><code>$secondary</code></td>
      <td align="center">#80869b</td>
      <td><div style={{background: '#80869b', width: '90px', height: '25px', display: 'inline-block'}}/></td>
    </tr>
    <tr>
      <td align="center">Secondary color - darker</td>
      <td align="center"><code>$secondary-darker</code></td>
      <td align="center">#505565</td>
      <td><div style={{background: '#505565', width: '90px', height: '25px', display: 'inline-block'}}/></td>
    </tr>
    <tr>
      <td align="center">Light</td>
      <td align="center"><code>$light</code></td>
      <td align="center">#e9ebf2</td>
      <td><div style={{background: '#e9ebf2', width: '90px', height: '25px', display: 'inline-block'}}/></td>
    </tr>
    <tr>
      <td align="center">Light lighter</td>
      <td align="center"><code>$light-lighter</code></td>
      <td align="center">#f8f9fb</td>
      <td><div style={{background: '#f8f9fb', width: '90px', height: '25px', display: 'inline-block'}}/></td>
    </tr>
    <tr>
      <td align="center">Light darker</td>
      <td align="center"><code>$light-darker</code></td>
      <td align="center">#dbdde5</td>
      <td><div style={{background: '#dbdde5', width: '90px', height: '25px', display: 'inline-block'}}/></td>
    </tr>
    <tr>
      <td align="center">Dark</td>
      <td align="center"><code>$dark</code></td>
      <td align="center">#5b6176</td>
      <td><div style={{background: '#5b6176', width: '90px', height: '25px', display: 'inline-block'}}/></td>
    </tr>
  </tbody>
</table>

In case you are using the colors in the frontend, you can access them via the theme variables file. For styled components, it works like this:

```javascript
const ExampleStyled = styled.div`
    color: ${(props) => props.theme.primary};
`;
```
