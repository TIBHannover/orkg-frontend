# Introduction

Here we document some of the components used in the [ORKG Frontend](https://orkg.org). The scope of this component library includes:

-   components that are reusable (i.e., they are made to be used more than once in the codebase)
-   components that are usable in isolation (i.e., not relying on a global store)

The API endpoints from [sandbox.orkg.org](sandbox.orkg.org) are used in the stories to ensure the components are functional and display actual data. API endpoints are not mocked.

## Bootstrap components

The ORKG frontend relies mainly on [Bootstrap](https://getbootstrap.com/) using [Reactstrap](https://reactstrap.github.io/). We do not duplicate the documentation from Reactstrap, so please refer to their documentation to find the majority of components used in the ORKG. We always recommend to use their components instead of creating new ones yourself. Only if components are missing, or are not well suited, we create our own components.

## Color palette

|           Name           |    Variable name    |  Color  | Preview                                                                                        |
| :----------------------: | :-----------------: | :-----: | ---------------------------------------------------------------------------------------------- |
|      Primary color       |     `$primary`      | #e86161 | <div style={{background: '#e86161', width: '90px', height: '25px', display: 'inline-block'}}/> |
|  Primary color - darker  |  `$primary-darker`  | #c61d1d | <div style={{background: '#c61d1d', width: '90px', height: '25px', display: 'inline-block'}}/> |
|     Secondary color      |    `$secondary`     | #80869b | <div style={{background: '#80869b', width: '90px', height: '25px', display: 'inline-block'}}/> |
| Secondary color - darker | `$secondary-darker` | #505565 | <div style={{background: '#505565', width: '90px', height: '25px', display: 'inline-block'}}/> |
|          Light           |      `$light`       | #e9ebf2 | <div style={{background: '#e9ebf2', width: '90px', height: '25px', display: 'inline-block'}}/> |
|      Light lighter       |  `$light-lighter`   | #f8f9fb | <div style={{background: '#f8f9fb', width: '90px', height: '25px', display: 'inline-block'}}/> |
|       Light darker       |   `$light-darker`   | #dbdde5 | <div style={{background: '#dbdde5', width: '90px', height: '25px', display: 'inline-block'}}/> |
|           Dark           |       `$dark`       | #5b6176 | <div style={{background: '#5b6176', width: '90px', height: '25px', display: 'inline-block'}}/> |

In case you are using the colors in the frontend, you can access them via the theme variables file. For styled components, it works like this:

```javascript
const ExampleStyled = styled.div`
    color: ${(props) => props.theme.primary};
`;
```
